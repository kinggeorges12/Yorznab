# Roadmap
1. When configuring app integrations, users enter the URL then are provided with a link to open the API settings for that app. It should dynamically update, starting from the default localhost:port/settings/general and then after entering a url, updates to the new url location.
2. Test the qBittorrent search to retrieve available tags and provide those as suggestions in the YAML Editor